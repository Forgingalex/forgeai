# FastAPI & Server
fastapi>=0.109.0  # Pydantic v2 compatible
uvicorn[standard]==0.24.0
websockets==12.0
python-multipart==0.0.6

# Database
sqlalchemy>=2.0.25  # Python 3.13 compatible
alembic>=1.13.0
psycopg[binary]>=3.2.2  # Python 3.13 compatible
# asyncpg==0.29.0  # Optional - requires C++ build tools on Windows. Install separately if needed.

# Redis
redis==5.0.1
# hiredis==2.2.3  # Optional - requires C++ build tools on Windows

# Background Tasks
celery==5.3.4
flower==2.0.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<5.0.0  # Compatible with passlib 1.7.4 (bcrypt 5.0+ has compatibility issues)
python-multipart==0.0.6

# AI & ML - Ollama (Free, Local)
openai==1.3.5  # Optional fallback
pypdf==3.17.4
python-pptx==0.6.23
pillow==10.4.0
numpy>=1.19.5  # Python 3.13 requires numpy 2.x
scikit-learn==1.5.2

# RAG
llama-index==0.9.20
langchain>=0.1.0  # Updated for numpy 2.x compatibility

# File Processing
python-magic==0.4.27
filetype==1.2.0

# Utilities
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.0.0  # Required for Pydantic EmailStr validation
python-dotenv==1.0.0
httpx==0.25.2
aiofiles==23.2.1

# Monitoring
prometheus-client==0.19.0

